{
  "name": "macheck-app",
  "version": "1.0.0",
  "description": "MaCheck App - Google Competition Stack",
  "private": true,
  "scripts": {
    "typecheck": "npm --prefix mobile run typecheck && npm --prefix functions run build"
  }
}
